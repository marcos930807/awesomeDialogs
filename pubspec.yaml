name: awesome_dialog
description: >-
  Flutter package to show beautiful dialogs(INFO,QUESTION,WARNING,SUCCESS,ERROR)
  with animations as simply as possible.
version: 2.2.1
homepage: https://github.com/marcos930807/awesomeDialogs

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flare_flutter: ^3.0.2
  flutter:
    sdk: flutter
  simple_animations: ^4.0.1

dev_dependencies:
  flutter_lints: ^1.0.4
  flutter_test:
    sdk: flutter
  lint: ^1.8.2

flutter:
  assets:
    - assets/flare/
